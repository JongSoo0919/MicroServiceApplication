server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file://C:\PROJECT\MicroServiceApplication\config-server-file\local-repo